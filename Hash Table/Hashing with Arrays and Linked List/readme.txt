hash.cpp ->implements hashing, Creates an array and stores the address of a node and traverses through it based on user input of the hash table.
rand_hash.cpp ->implements hashing, Creates an array and stores the address of a node and traverses through it based on random number generated.
